services:
  control:
    build:
      dockerfile: ./dockerfiles/Dockerfile.control
    volumes:
      - ./ansible:/ansible
    tty: true
    stdin_open: true
    working_dir: /ansible
    networks:
      - ansible-sandbox

  managed:
    build:
      dockerfile: ./dockerfiles/Dockerfile.managed
    deploy:
      mode: replicated
      replicas: 2
    networks:
      - ansible-sandbox

networks:
  ansible-sandbox:
